apiVersion: apps/v1
kind: Deployment
metadata:
  name: questify-auth-mongo-depl
spec:
  replicas: 1
  selector:
    matchLabels:
      app: questify-auth-mongo
  template:
    metadata:
      labels:
        app: questify-auth-mongo
    spec:
      containers:
        - name: questify-auth-mongo
          image: mongo
---
apiVersion: v1
kind: Service
metadata:
  name: questify-auth-mongo-srv
spec:
  selector:
    app: questify-auth-mongo
  ports:
    - name: db
      protocol: TCP
      port: 27017
      targetPort: 27017
