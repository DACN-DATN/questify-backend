apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: cm-postgres-volume-claim
  labels:
    app: cm-postgres
spec:
  storageClassName: manual
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 10Gi
